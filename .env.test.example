# Copy to .env.test and configure for test database
# Tests use room_booking_test by default

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD='yourpassword'
DB_NAME=room_booking_test
JWT_SECRET=test-jwt-secret
